v = float(input('Quantos reais voçê gostaria de converte em dolar ? R$'))
d = v // 5.64 
if v >= 5.64:
 {
print (f'Com R${v} voçê pode comprar U${d}')

  } 
else: { 
 print('voçê não pode comprar nenhum dolar!')
  }